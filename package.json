{
  "name": "nesheim-vatten",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "lint": "stylelint \"**/*.css\" && htmllint clients/**/*.html docs/**/*.html"
  },
  "devDependencies": {
    "htmllint-cli": "^0.0.7",
    "jest": "^29.0.0",
    "jsdom": "^22.0.0",
    "stylelint": "^16.22.0",
    "stylelint-config-standard": "^38.0.0"
  }
}
