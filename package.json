{
  "name": "nesheim-vatten",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "lint": "npm run lint:css && npm run lint:html",
    "lint:css": "stylelint '**/*.css'",
    "lint:html": "htmllint **/*.html **/*.liquid"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^22.0.0",
    "stylelint": "^16.22.0",
    "htmlhint": "^1.6.3"
  }
}
