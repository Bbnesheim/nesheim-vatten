{
  "name": "nesheim-vatten",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "lint": "npm run lint:css && npm run lint:html",
  },
  "devDependencies": {
    "htmllint-cli": "^0.0.7",
    "htmllint": "^0.7.3",
    "jest": "^29.0.0",
    "jsdom": "^22.0.0",
    "stylelint": "^16.22.0",
  }
}
